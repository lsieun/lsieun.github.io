---
title: "IP"
sequence: "101"
---

## 从数字转换成IP

### 使用 int 还是 long

将 `255.255.255.255` 转换成十进制之后的值：

```text
4294967295 (这是 255.255.255.255 的值)
2147483647 (这是 Integer.MAX_VALUE 的值)
```

因此，需要使用 `long` 类型。

### 使用 `long` 类型

## IP 的有效性检查

## IP 地址的分类
