---
title:  "JVM Execution Model"
sequence: "103"
---


this，方法参数，局部变量,

long和double类型占用2个slot的位置

`boolean`, `byte`, `short`, `char`, or `int`，这几种类型，在local variable和operand stack当中，都是作为`int`类型进行处理。

Word

local variable和operand stack的一个空间的大小，又称之为slot。


## HelloWorldFrameCore

输出结果的理解。
